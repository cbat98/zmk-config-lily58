on:
  push:
    branches:
      - master
      - test
  pull_request:
    branches:
      - master
  pull_request_target:
    branches:
      - master
jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
